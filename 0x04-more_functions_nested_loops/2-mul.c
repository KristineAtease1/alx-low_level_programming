#include "main.h"
#include <stdio.h>

/**
 * mul - function that will multiply two integers
 * @a: First integer
 * @b: Second integer
 * Return: mul.
 */
int mul(int a, int b)
{
int x;

x = a * b;
return (x);
}
